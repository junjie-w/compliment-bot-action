{
  "name": "compliment-bot-action",
  "version": "1.2.0",
  "description": "Generates a random, enthusiastic compliment with emojis on every push.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --coverage --ci",
    "lint": "eslint \"src/**/*.js\" \".github/**/*.yml\"",
    "lint:fix": "eslint \"src/**/*.js\" \".github/**/*.yml\" --fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/junjie-w/compliment-bot-action.git"
  },
  "author": "Junjie Wu",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/junjie-w/compliment-bot-action/issues"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@vercel/ncc": "^0.38.2",
    "eslint": "^8.57.1",
    "eslint-plugin-import": "^2.31.0",
    "eslint-plugin-yml": "^1.14.0",
    "jest": "^29.7.0",
    "semantic-release": "^24.1.3"
  }
}
